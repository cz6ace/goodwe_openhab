# goodwe_openhab
GoodWe OpenHAB integration using MQTT


# Possible installation/usage:
```
python3 -m venv venv
. venv/bin/activate
pip install -e .
gw --help
```

```
pipx install -e . --force
gw --help
```
